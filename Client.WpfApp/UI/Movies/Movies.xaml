<UserControl x:Class="Client.WpfApp.UI.Movies.Movies"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView ItemsSource="{Binding Movies}">
        <ListView.ItemTemplate>
            <DataTemplate x:Name="MovieDataTemplate">
                <StackPanel Height="84">
                    <TextBlock Text="{Binding Title}" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding Year}" Opacity=".8" Margin="0,4,0,0"/>
                    <TextBlock Text="{Binding ImdbId}" Opacity=".6" Margin="0,4,0,0"/>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
